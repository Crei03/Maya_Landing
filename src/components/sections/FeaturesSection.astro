<section id="funcionalidades" class="py-24 bg-surface-container">
	<div class="max-w-7xl mx-auto px-8">
		<div class="flex flex-col md:flex-row justify-between items-end gap-8 mb-16">
			<div class="max-w-2xl">
				<h2 class="font-headline text-4xl font-extrabold tracking-tight">Funcionalidades clave para operaciones logísticas de última milla</h2>
				<p class="text-on-surface-variant mt-4">Todo lo que tu equipo necesita para vender mejor, entregar más rápido y mantener clientes satisfechos sin procesos complicados.</p>
			</div>
			<button class="bg-primary text-white px-6 py-3 rounded-xl font-bold active:scale-95 transition-transform">Ver funcionalidades</button>
		</div>
		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
			<div class="p-8 bg-surface-container-lowest rounded-xl border-l-4 border-primary">
				<span class="material-symbols-outlined text-primary mb-4">api</span>
				<h3 class="font-bold mb-2">Conecta tus sistemas sin fricción</h3>
				<p class="text-sm text-on-surface-variant">Integra pedidos, inventario y clientes en un solo flujo para que tu equipo opere más rápido.</p>
			</div>
			<div class="p-8 bg-surface-container-lowest rounded-xl">
				<span class="material-symbols-outlined text-primary mb-4">map</span>
				<h3 class="font-bold mb-2">Entregas con rutas inteligentes</h3>
				<p class="text-sm text-on-surface-variant">Recibe recomendaciones de ruta y alertas en tiempo real para evitar retrasos y mejorar la puntualidad.</p>
			</div>
			<div class="p-8 bg-surface-container-lowest rounded-xl">
				<span class="material-symbols-outlined text-primary mb-4">hub</span>
				<h3 class="font-bold mb-2">Todos tus pedidos en un solo panel</h3>
				<p class="text-sm text-on-surface-variant">Centraliza ventas de diferentes canales y mejora la coordinación entre bodega, despacho y transporte.</p>
			</div>
			<div class="p-8 bg-surface-container-lowest rounded-xl">
				<span class="material-symbols-outlined text-primary mb-4">shield_with_heart</span>
				<h3 class="font-bold mb-2">Tu operación segura y respaldada</h3>
				<p class="text-sm text-on-surface-variant">Protege datos de clientes y entregas con controles de acceso y respaldo continuo de información.</p>
			</div>
		</div>
	</div>
</section>
