---
interface Props {
	title: string;
	description: string;
	href: string;
}

const { title, description, href } = Astro.props;
---

<a class="card link-card reveal" href={href}>
	<h3>{title}</h3>
	<p>{description}</p>
</a>
